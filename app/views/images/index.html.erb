<p id="notice"><%= notice %></p>

<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-4">Digitizing Black History</h1>
    <p class="lead">Sharing our African American Images</p>
  </div>
</div>
<%= link_to 'New Image', new_image_path %>
<div class="row">   
  <div class="container">
          <div class="row">
            <% @images.each do |image| %>
              <div class="col-sm-6 col-md-4 col-lg-3">
               <%# if @image.photo.attached? %>
                <%= link_to image_tag(image.photo.variant(resize: '300x300!')), image %>
                <%# end %>
                <%#= image.title %>
                <%#= link_to 'Show', image %>
                <%#= link_to 'Edit', edit_image_path(image) %>
                <%#= link_to 'Destroy', image, method: :delete, data: { confirm: 'Are you sure?' } %>
              </div>
            <% end %>
          </div>
    <br>
    <div class="apple_pagination">
      <%= will_paginate @images, container: false %>
    </div>
<!-- Comment for Jan 2020 presentation
    <h1>
      <center>Most Recent Images</center>
    </h1>
    <div class="row">
            <%# @imagesrecent.each do |image| %>
              <div class="col-sm-6 col-md-4 col-lg-3">
                <%#= link_to image_tag(image.photo.variant(resize: '300x300!')), image %>
              </div>
            <%# end %>
          </div>
-->
  </div> <!-- container -->
</div> <!-- row -->

<%# if user_signed_in? %>
  <%#= link_to 'New Image', new_image_path %>
<%# end %>
